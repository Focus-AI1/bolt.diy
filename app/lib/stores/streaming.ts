import { atom } from 'nanostores';

export const streamingState = atom<boolean>(false);
export const prdStreamingState = atom<boolean>(false);
export const researchStreamingState = atom<boolean>(false);
export const ticketStreamingState = atom<boolean>(false);
